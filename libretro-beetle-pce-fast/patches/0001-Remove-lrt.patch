diff --git a/Makefile b/Makefile
index 4aca3b3..a15d82a 100644
--- a/Makefile
+++ b/Makefile
@@ -77,7 +77,6 @@ ifneq (,$(findstring unix,$(platform)))
    LDFLAGS += -lroot
    CXXFLAGS += -fpermissive
    else
-   LDFLAGS += -lrt
    endif
    
    ifneq ($(findstring Linux,$(shell uname -s)),)
